{
    "layer": "top", // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    "height": 34, // Waybar height (to be removed for auto height)
    "spacing": 4, // Gaps between modules (4px)
    "modules-left": ["wlr/workspaces"],
    "modules-center": ["mpd"],
    "modules-right": ["pulseaudio", "battery", "custom/clock"],
    "wlr/workspaces": {
        "format": " {icon} ",
        "format-active": " {icon} ",
        "on-click": "activate",
        "active-only": false,
        "all-outputs": true
    },
    "mpd": {
        "format": "{stateIcon} 󰎉 {artist} - {title} ",
        "format-disconnected": "Disconnected 󰎉",
        "format-stopped": "Stopped 󰎉",
        "unknown-tag": "N/A",
        "interval": 2,
        "state-icons": {
            "paused": "paused ",
            "playing": "playing "
        },
        "tooltip-format": "MPD (connected)",
        "tooltip-format-disconnected": "MPD (disconnected)"
    },
    "custom/clock": {
        "exec": "date +' %-l:%M %P '",
        "interval": 1
    },
    "battery": {
        "format": "battery {icon} {capacity}",
        "format-charging": "charging {icon} {capacity}",
        "format-plugged": "battery {icon} {capacity}",
        "format-alt": "{time} {icon}",
        "format-icons": [" ", " ", " ", " ", " "]
    },
    "pulseaudio": {
        "scroll-step": 0.1, // %, can be a float
        "format": "volume {icon} {volume}",
        "format-bluetooth": "volume {icon} {volume} ",
        "format-bluetooth-muted": "muted  {icon} ",
        "format-muted": "muted  ",
        "format-icons": {
            "default": ["", "", " "]
        },
        "on-click": "pavucontrol"
    },
}

